services:
  app:
    build:
      context: .
      args:
        NODE_ENV: development
        DATABASE_URL: "file:/app/data/dev.db"
    ports:
      - "3000:3000"
    env_file: .env.dev
    volumes:
      - dev-data:/app/data
    restart: unless-stopped

volumes:
  dev-data:
