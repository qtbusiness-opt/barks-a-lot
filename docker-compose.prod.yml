services:
  app:
    build:
      context: .
      args:
        NODE_ENV: production
        DATABASE_URL: "file:/app/data/prod.db"
    ports:
      - "3002:3000"
    env_file: .env.prod
    volumes:
      - prod-data:/app/data
    restart: unless-stopped

volumes:
  prod-data:
