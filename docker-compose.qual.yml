services:
  app:
    build:
      context: .
      args:
        NODE_ENV: production
        DATABASE_URL: "file:/app/data/qual.db"
    ports:
      - "3001:3000"
    env_file: .env.qual
    volumes:
      - qual-data:/app/data
    restart: unless-stopped

volumes:
  qual-data:
